FROM python:3.12-slim

WORKDIR /app

COPY . /app

RUN pip install --no-cache-dir fastmcp pyyaml

EXPOSE 8001

CMD ["fastmcp", "run", "server.py", "--transport", "http", "--host", "0.0.0.0", "--port", "8001"]